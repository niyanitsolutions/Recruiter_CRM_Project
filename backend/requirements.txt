# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database — MongoDB async driver
motor==3.3.2
pymongo==4.6.1

# Redis — caching, sessions, rate limiting
redis[asyncio]==5.0.1
hiredis==2.3.2

# Rate limiting
slowapi==0.1.9
limits==3.12.0

# Secrets encryption
cryptography>=42.0.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0

# HTTP Client
httpx==0.26.0

# Razorpay SDK
# setuptools is required by razorpay==1.4.1 (imports pkg_resources at module load)
# Not included in python:3.12-slim or Python 3.12 by default
setuptools>=68.0.0
razorpay==1.4.1

# Utilities
python-dotenv==1.0.0
# bson is bundled with pymongo — do NOT install the standalone bson package

# AWS — S3 file uploads (resumes, documents)
boto3==1.34.34

# PDF Generation
reportlab==4.1.0

# Excel / Spreadsheet
openpyxl==3.1.2
# xlrd reads legacy .xls files (xlrd 2.x supports .xls only; .xlsx uses openpyxl)
xlrd==2.0.1

# Resume Parsing (text extraction)
pypdf==4.2.0
python-docx==1.1.2

# AI / Provider SDKs (multi-provider support)
anthropic==0.40.0
openai>=1.30.0
# google-generativeai removed — GeminiAdapter now uses httpx REST directly (no SDK needed)

# Date/Time
python-dateutil==2.8.2

# Logging
loguru==0.7.2

# Error tracking — optional at runtime: only initialized when SENTRY_DSN is set
# (see app/main.py). Includes FastAPI/Starlette integration.
sentry-sdk[fastapi]>=2.8,<3

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# Development / Linting
black==24.1.0
isort==5.13.2
flake8==7.0.0
